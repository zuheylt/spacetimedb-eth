[package]
name        = "token_transfer"
description = "Example: watch ERC-20 Transfer events and record them in SpacetimeDB"
version.workspace     = true
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true

[[bin]]
name = "token_transfer"
path = "src/main.rs"

[dependencies]
eth-core   = { path = "../../crates/eth-core" }
eth-module = { path = "../../crates/eth-module" }
